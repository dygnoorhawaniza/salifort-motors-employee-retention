# 🧸 Source code

The original supplied artifact is preserved in `../notebooks/`.

This folder is intentionally kept lightweight because the provided project is notebook-based. If you later convert the project into a production-style Python workflow, this is a good place to add modules such as:

- `data_cleaning.py`
- `eda.py`
- `features.py`
- `train_model.py`
- `evaluate_model.py`

That would make the analysis easier to reproduce outside Jupyter.
